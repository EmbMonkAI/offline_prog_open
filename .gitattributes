# 换行符统一（跨平台协作）：文本文件按规范入库，检出按平台
* text=auto

# Windows 专属工程文件保持 CRLF
*.bat text eol=crlf

# Keil 工程文件（XML，保持原样）
*.uvprojx text
*.uvoptx text
*.ioc text

# 二进制文件不做任何转换
*.pdf binary
*.bin binary
*.png binary
*.hex binary
